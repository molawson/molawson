<% title @post.title %>

<div id="main">
  <article>
  <header>
    <h2><%= @post.title %></h2>
    <p class="meta"><%= "Posted on " + @post.created_at.strftime("%B %e, %Y") %></p>
  </header>  
    <%= textilize(@post.content) %>    
  </article>
</div><!-- end main -->

<%= render "blog_sidebar" %>